name: flutterish_image_cropper
description: A pure Dart/Flutter image cropper with no native dependencies. Supports interactive cropping with gestures, rotation, and aspect ratio constraints.
version: 0.1.0
homepage: https://github.com/evaisse/flutterish/tree/main/packages/flutterish_image_cropper
repository: https://github.com/evaisse/flutterish
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  image: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
